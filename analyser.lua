local analyzer = {}

function analyzer.analyzeData(humidity, rain, plant_type)
    -- Insert algorithm to analyze the data collected and compute a value that will tell if the plant should be watered or not
end
